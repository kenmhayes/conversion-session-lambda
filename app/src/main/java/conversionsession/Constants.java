package conversionsession;

public class Constants {
    // Needed for the dynamodb mapper annotation, otherwise would be an environment variable
    public static final String SESSION_INDEX_NAME = "Session";
}
